<h2 id="chat__title">Chat</h2>
<div id="chat_dashboard">
    {{#if messages.length}}
        {{#each messages}}
            <div class="chat">
                <p>{{this.message}}</p>
                <p>{{this.createdAt}}</p>
            </div>
        {{/each}}
    {{/if}}
</div>